// Stub for `server-only` in the Vitest environment.
// In Next.js builds the real module throws when imported from a client
// component; in Node-based tests we don't need that guard, so this empty
// module is a safe no-op.
export {}
